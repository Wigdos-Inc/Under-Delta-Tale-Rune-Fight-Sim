# Railway configuration for FRONTEND static file server
# This serves index.html, css/, src/, data/ folders

[build]
builder = "nixpacks"

[deploy]
startCommand = "npx http-server -p $PORT -c-1 --cors"
healthcheckPath = "/"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
